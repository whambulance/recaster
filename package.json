{
  "name": "@whambulance/recaster",
  "version": "1.1.1",
  "description": "Determine 2d laser reflection inside a polygon",
  "main": "src/recaster.ts",
  "types": "src/interfaces/index.ts",
  "scripts": {
    "test": "jest --config jestconfig.json",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/whambulance/enigma-machine.git"
  },
  "keywords": [
    "laser-reflection",
    "laser",
    "raycast",
    "polygon",
    "interior",
    "2d"
  ],
  "author": "whambulance",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/whambulance/enigma-machine/issues"
  },
  "homepage": "https://github.com/whambulance/enigma-machine#readme",
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "jest": "^27.4.7",
    "prettier": "^2.5.1",
    "ts-jest": "^27.1.2",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.5.4"
  },
  "imports": {
    "@/*": "./src/*"
  },
  "dependencies": {
    "timsort": "^0.3.0"
  }
}
